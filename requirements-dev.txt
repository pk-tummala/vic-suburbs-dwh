-r requirements.txt
pytest>=8.0
pytest-cov>=5.0
ruff==0.5.7        # keep in sync with .pre-commit-config.yaml rev
pre-commit>=3.7
# For local PySpark-based integration tests (optional; skipped if unavailable)
pyspark>=3.5 ; python_version < "3.13"
